describe('scrollify', function() {

  // beforeEach(() => {
  //   window.innerHeight =
  //   window.pageYOffset = value || 0;
  // })


	it('initializes correctly', function () {

	});

	it('accepts the correct options', function () {
		// range, mode, el
	});


  it('parses correct values', function() {
    it('as a number', function() {}); // `0.3`
    it('as a pixel', function() {});  // `200px`
    it('as a viewport unit', function() {}); // `40vh`
    it('relative to `element`', function() {}); // `(el) => el.top`
    it('with multiple units', function() {}); // `(el) => el.top - 30vh` / `(el) => el.bottom - 30vh`
    it('with `refs`', function() {}); // `...`

  });


  it('calculates current state', function() { // before/after/active
    // before()
  });



});
