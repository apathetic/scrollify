describe('parallax', function() {

	it('should should export to the window object', function () {
		// expect(...
	});

	it('should should export as a common module', function () {

	});

	it('should should export as an AMD module', function () {

	});

	it('initializes correctly', function () {

	});

	it('accepts the correct options', function () {
		// range, mode, el
	});

});
